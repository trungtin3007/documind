# Everything in requirements.txt, plus what is needed to build a corpus,
# run the eval harness, or serve with DOCUMIND_METHOD=hybrid-embedding.
#
# Install with:  pip install -r requirements-dev.txt

-r requirements.txt
# pymupdf and pillow now live in requirements.txt: /upload needs them at run time.

# Semantic text scoring (hybrid-embedding). Pulls torch; on Linux install the
# CPU wheel explicitly or pip fetches multi-GB CUDA builds:
#   pip install torch==2.13.0 --index-url https://download.pytorch.org/whl/cpu
sentence-transformers==5.7.0
